<%= form_with(model: elastic_report, local: true) do |form| %>
  <% if elastic_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(elastic_report.errors.count, "error") %> prohibited this elastic_report from being saved:</h2>

      <ul>
      <% elastic_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :report_type_id %>
    <%= form.number_field :report_type_id %>
  </div>

  <div class="field">
    <%= form.label :report_value_id %>
    <%= form.number_field :report_value_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
